let numberArray = []

//buisness logic
function translator(number) {
  let numberInt = parseInt(number);
  for (var number i = 0; i <= numberInt; i+=1);
  const numbers = (""+index).split("")
  if (index === 3 || numbers.includes("3")) {
    outputArray.push("Won't your be my neighbor?")
    continue
  }
  //let numberArray = ["0", "beep", "boop", "wont you be my neighbor", "4", "5"];

  //if (number === 13) {
    //return "wont you be my neighbor"
  //} else if (number === 21) {
    //return "boop"
  //} else if (number === 32) {
   // return "wont you be my neighbor"
  //} 



}
